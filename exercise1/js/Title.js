class Title extends State {
  constructor() {
    super();
  }

  draw() {
    super.draw();
  }

  text() {
    // push();
    // fill(0, 127, 255);
    // translate(0, -275, 250);
    // textFont(font);
    // textSize(70);
    // textAlign(CENTER, CENTER);
    // text(`Field of Artificial Flowers`, 0, 0);
    // pop();
  }

  keyReleased() {
    super.keyReleased();
  }

  keyPressed() {
    super.keyPressed();
  }
}
